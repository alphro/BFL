# PURPOSE:
# Extracts posterior summaries (phi) from an LCVA fit.
# This isolates all posterior-averaging logic in one place.

extract_posterior_phi <- function(lcva_fit) {
  stop("extract_posterior_phi not implemented yet")
}
