# PURPOSE:
# Applies global BFL parameters to local X to compute
# per-individual predictions and prevalence estimates.

predict_with_bfl <- function(
    global_fit,
    X
) {
  stop("predict_with_bfl not implemented yet")
}
