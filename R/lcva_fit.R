# PURPOSE:
# Thin wrapper around LCVA that fits the model and returns the fit object.
# No data munging. No summaries. Just fitting.

fit_lcva <- function(X, Y = NULL, K, lcva_args) {
  stop("fit_lcva not implemented yet")
}
