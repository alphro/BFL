% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BFL_methods.R
\name{site_similarity_BFL}
\alias{site_similarity_BFL}
\title{Pairwise site similarity from a BFL fit}
\usage{
site_similarity_BFL(fit, plot = FALSE)
}
\arguments{
\item{fit}{Object of class \code{"BFL"} returned by \code{run_BFL()}.}

\item{plot}{Logical; if \code{TRUE}, prints a \code{ggplot2} heatmap of
the correlation matrix. Default \code{FALSE}.}
}
\value{
An M \eqn{\times} M correlation matrix named by
\code{fit$model_names}, returned invisibly. If \code{plot = TRUE},
the heatmap is also printed.
}
\description{
Computes pairwise Pearson correlations between local models using their
posterior-mean phi scores (per-record class probabilities) flattened
across all N observations and C causes.
}
\details{
High correlation between two sites means their local models assign similar
scores to each target record — they are contributing redundant information
to the ensemble. Low correlation means the sites are complementary.
This is useful for diagnosing whether adding a site actually helps.
}
\seealso{
\code{\link{run_BFL}}, \code{\link{summary.BFL}}
}
