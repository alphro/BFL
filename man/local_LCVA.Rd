% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_LCVA.R
\name{local_LCVA}
\alias{local_LCVA}
\title{Fit a local LCVA model and summarize predictions on a target set}
\usage{
local_LCVA(X_train, Y_train, X_target, lcva_args = list())
}
\arguments{
\item{X_train}{Numeric matrix/data.frame of training symptoms (N_train x P).}

\item{Y_train}{Vector of training cause labels (length N_train).}

\item{X_target}{Numeric matrix/data.frame of target symptoms (N_target x P).}

\item{lcva_args}{List of arguments controlling LCVA (e.g., K, Nitr, thin, seed, burn_in).}
}
\value{
A list with components:
\describe{
\item{posterior_phi}{Numeric matrix (N_target x C_site) of posterior mean likelihoods.}
\item{cause_ids}{Character vector of cause labels corresponding to columns of posterior_phi.}
\item{target_info}{List with \code{N}, \code{P}, an order-sensitive dataset \code{hash},
and \code{row_hash} (length N) for row-wise alignment across permutations.}
}
}
\description{
Trains an LCVA model on labeled training data and returns a local summary
evaluated on a target dataset (no target labels required). The output is a
compact object used by BFL global aggregation.
}
