% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{gibbs_bfl_cpp}
\alias{gibbs_bfl_cpp}
\title{Unified Gibbs sampler for all three BFL variants}
\usage{
gibbs_bfl_cpp(
  phi,
  model_presence,
  causes_r,
  Y_known,
  Y_idx_r,
  N,
  C_max,
  M,
  num_causes,
  n_iter,
  n_warmup,
  single_pi = TRUE,
  logistic_normal = FALSE,
  mh_scale = 0.5
)
}
\arguments{
\item{phi}{Flattened phi array (column-major, dim N x C_max x M).}

\item{model_presence}{num_causes x M integer matrix; 1 = model active.}

\item{causes_r}{1-indexed cause codes, length num_causes.}

\item{Y_known}{Integer vector length N: 1 = cause observed, 0 = latent.
Pass \code{integer(N)} for the no-partial variant.}

\item{Y_idx_r}{Integer vector length N: 1-indexed cause index used
where Y_known = 1. Pass \code{rep(1L, N)} for the no-partial variant.}

\item{N, C_max, M, num_causes}{Array dimensions.}

\item{n_iter}{Total iterations (warmup + sampling).}

\item{n_warmup}{Iterations to discard.}

\item{single_pi}{TRUE = one pi for all observations (no_partial and
balanced); FALSE = separate pi (unlabeled) and pi_O (labeled).}

\item{logistic_normal}{If TRUE, use N(0,1)->softmax prior with MH step.}

\item{mh_scale}{Random-walk MH step-size (only used when logistic_normal=TRUE).}
}
\value{
List: pi \link{n_keep x num_causes}, pi_O \link{n_keep x num_causes} (zeros
when single_pi=TRUE), lambda ,
mh_accept_rate \link{num_causes} (NA when logistic_normal=FALSE).
}
\description{
Run one MCMC chain. Call from R once per chain with a different seed.
Pass \code{Y_known = integer(N)} and \code{Y_idx_r = rep(1L, N)} for the
no-partial-labels variant (no cause is observed, so the Y arguments are
never used).
}
\keyword{internal}
