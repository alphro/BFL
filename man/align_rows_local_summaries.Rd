% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_alignment.R
\name{align_rows_local_summaries}
\alias{align_rows_local_summaries}
\title{Align all local summaries to a reference row order}
\usage{
align_rows_local_summaries(local_summaries, ref_row_hash)
}
\arguments{
\item{local_summaries}{Named list of local model summary objects. Each
element must contain \code{posterior_phi} (N x C matrix) and
\code{target_info$row_hash} (character vector of length N).}

\item{ref_row_hash}{Character vector giving the canonical row hash order
(produced by \code{compute_row_hashes()}). Must be a permutation of
each site's \code{target_info$row_hash}.}
}
\value{
The same list structure as \code{local_summaries} with rows of
\code{posterior_phi} permuted to match \code{ref_row_hash}.
}
\description{
Re-orders the \code{posterior_phi} rows of every local summary so they
match the canonical target row order given by \code{ref_row_hash}. Also
updates \code{target_info$row_hash} and \code{target_info$N} in each
element.
}
\keyword{internal}
